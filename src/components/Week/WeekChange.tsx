import React from 'react';
import styled from 'styled-components';

function WeekInfo() {
  return <Styled.Root>일정 조절 버튼</Styled.Root>;
}

export default WeekInfo;

const Styled = {
  Root: styled.div``,
};
