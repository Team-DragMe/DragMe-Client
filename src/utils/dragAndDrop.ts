// drop ref inside daily
export const movePlanChipInsideDaily = ({ type }) => {};
// type별로 이동 호출 함수 다르게
// type을 daily로 바꾸고 orderIndex계산해서 중간으로 들어가도록
// dropRef가 li에 달려야
export const movePlanChipToDaily = ({ type }) => {};
// type을 reschedule로 바꾸고 가장 마지막에 추가되도록
export const movePlanChipToReschedule = ({ type }) => {};
// type을 routine으로 바꾸고 가장 마지막에 추가되도록
export const movePlanChipToRoutine = ({ type }) => {};
