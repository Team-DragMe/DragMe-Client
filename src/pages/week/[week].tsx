import React from 'react';
import styled from 'styled-components';

function Week() {
  return <Styled.Root>마이페이지입니다.</Styled.Root>;
}

export default Week;

const Styled = {
  Root: styled.div``,
};
