export interface StartDateQuery {
  startDate: string;
}

export interface startAndEndDate {
  startDate: string;
  endDate: string;
}

export interface scheduleAndDate {
  scheduleId: string;
  date: string;
}
